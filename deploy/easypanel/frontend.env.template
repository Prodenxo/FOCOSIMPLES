# Cole no Easypanel → focosimples-web → Environment (NÃO use Build Args)
#
# Domínio customizado: focosimples.com.br (Domains → Add → CNAME/A no DNS)
#
# Backend: URL pública do serviço focosimples-api (Easypanel ou api.focosimples.com.br)

EXPO_PUBLIC_AUTH_MODE=local
EXPO_PUBLIC_APP_PRODUCT=focosimples
EXPO_PUBLIC_MEI_API_URL=https://<URL_BACKEND>
EXPO_PUBLIC_INVITE_APP_BASE_URL=https://focosimples.com.br
