/** Versão interna do Fiscal Engine (semântica própria — independente do layout NF-e). */
export const ENGINE_SCHEMA_VERSION = '3.1.0';

/** Alias público para documentação e audit logs. */
export const FISCAL_ENGINE_SCHEMA_VERSION = ENGINE_SCHEMA_VERSION;
