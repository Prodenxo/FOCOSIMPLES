/**
 * Constantes de rollout authoritative v3 — Fase 8A.
 */
import { FISCAL_ENGINE_SCHEMA_VERSION } from '../constants.js';

export const ROLLOUT_MODE = Object.freeze({
  LEGACY: 'LEGACY',
  SHADOW: 'SHADOW',
  CANARY: 'CANARY',
  AUTHORITATIVE: 'AUTHORITATIVE',
  PAUSED: 'PAUSED',
});

export const VALID_ROLLOUT_MODES = Object.freeze(Object.values(ROLLOUT_MODE));

export const AUTHORITY_ENGINE = Object.freeze({
  LEGACY: 'LEGACY',
  V3: 'V3',
  /** Fail-closed — tenant/request authoritative selecionado; fiscal V3 não pode degradar para legacy. */
  BLOCKED: 'BLOCKED',
});

export const AUTHORITY_DECISION_REASON = Object.freeze({
  MASTER_SWITCH_OFF: 'MASTER_SWITCH_OFF',
  TENANT_LEGACY_DEFAULT: 'TENANT_LEGACY_DEFAULT',
  TENANT_ROLLOUT_DISABLED: 'TENANT_ROLLOUT_DISABLED',
  TENANT_MODE_LEGACY: 'TENANT_MODE_LEGACY',
  TENANT_MODE_SHADOW: 'TENANT_MODE_SHADOW',
  TENANT_MODE_PAUSED: 'TENANT_MODE_PAUSED',
  INVALID_ROLLOUT_MODE: 'INVALID_ROLLOUT_MODE',
  CANARY_NOT_SELECTED: 'CANARY_NOT_SELECTED',
  READINESS_NOT_MET: 'READINESS_NOT_MET',
  NOT_READY_NO_PRODUCTION_RULES: 'NOT_READY_NO_PRODUCTION_RULES',
  NOT_READY_NO_ACCOUNTANT_CONFIG: 'NOT_READY_NO_ACCOUNTANT_CONFIG',
  DOCUMENT_NOT_ELIGIBLE: 'DOCUMENT_NOT_ELIGIBLE',
  PREFLIGHT_FAILED: 'PREFLIGHT_FAILED',
  AUTHORITATIVE_FISCAL_BLOCKED: 'AUTHORITATIVE_FISCAL_BLOCKED',
  AUTHORITATIVE_PERSISTENCE_UNAVAILABLE: 'AUTHORITATIVE_PERSISTENCE_UNAVAILABLE',
  AUTHORITATIVE_PROVIDER_BRIDGE_NOT_EXECUTABLE: 'AUTHORITATIVE_PROVIDER_BRIDGE_NOT_EXECUTABLE',
  AUTHORITATIVE_NOT_ELIGIBLE: 'AUTHORITATIVE_NOT_ELIGIBLE',
  V3_CANDIDATE: 'V3_CANDIDATE',
  V3_SELECTED: 'V3_SELECTED',
});

export const EMISSION_ATTEMPT_STATUS = Object.freeze({
  ROUTING_LEGACY: 'ROUTING_LEGACY',
  PREFLIGHT_FAILED: 'PREFLIGHT_FAILED',
  AUTHORITATIVE_NOT_ELIGIBLE: 'AUTHORITATIVE_NOT_ELIGIBLE',
  RESERVED: 'RESERVED',
  AUTHORITY_ASSUMED_V3: 'AUTHORITY_ASSUMED_V3',
  /** Reserva + decision persistidas — imediatamente antes de adapter.emitir (TX1 committed). */
  PREPARED: 'PREPARED',
  EMITTED: 'EMITTED',
  REJECTED: 'REJECTED',
  REQUEST_OUTCOME_UNKNOWN: 'REQUEST_OUTCOME_UNKNOWN',
  RELEASED: 'RELEASED',
  CONSUMED: 'CONSUMED',
});

export const RESERVATION_LIFECYCLE = Object.freeze({
  RESERVED: 'RESERVED',
  CONSUMED: 'CONSUMED',
  RELEASED: 'RELEASED',
  PENDING_SYNC: 'PENDING_SYNC',
});

export const REQUEST_OUTCOME = Object.freeze({
  SUCCESS: 'SUCCESS',
  REJECTED: 'REJECTED',
  NETWORK_ERROR: 'NETWORK_ERROR',
  UNKNOWN: 'UNKNOWN',
});

export const DEFAULT_ENGINE_VERSION = FISCAL_ENGINE_SCHEMA_VERSION;

export const DEFAULT_ROLLOUT_POLICY = Object.freeze({
  empresaId: null,
  mode: ROLLOUT_MODE.LEGACY,
  canaryPercentage: 0,
  enabled: false,
  engineVersion: DEFAULT_ENGINE_VERSION,
  minimumShadowSamples: 0,
  readinessRequired: true,
  reason: 'TENANT_LEGACY_DEFAULT',
  configured: false,
});

/** Documentos elegíveis para authoritative v3 nesta fase. */
export const AUTHORITATIVE_ELIGIBLE_DOCUMENT_TYPES = Object.freeze(['NFE', 'NFCE']);
