# Backend — Foco Simples

API Node + Express. Porta padrão: **3333**.

```bash
cp .env.example .env
npm install
npm run dev
```

Banco: Postgres no Easypanel. Schemas em `db/easypanel/`.

Testes: `npm test`
